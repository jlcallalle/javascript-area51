//Practicar 
https://bit.ly/2xyQAD8
//repaso clase3-ejemplo-profe


//REVIEW CLASE FUNCIONES

// ES5
var saltar = function (metros) {
	console.log('Salto ' + metros + ' metros');
};
//cuando una funcion no tiene nombre se conoce como una funcion anonima

//ES6
//si se unsa un parametro, puede o no mantener parentecis
const saltar =  metros => {
	console.log(`Salto ${metros} metros`);
};

//si vas a usar varios parametros si obligado los parentecis
const fullname =  (firstName, lastName) => {
	console.log(`${firstName} ${lastName} `);
};


// --
function sumar(a, b) {
  console.log(`La suma de ${a} + ${b} es ${a + b}`);
}
sumar(6, 2);

//Despues del Breake
//....

const alumnos = [
  { nombre: 'Pedro', edad: 23 },
  { nombre: 'Juan', edad: 21 },
  { nombre: 'Rosa', edad: 25 }
];

alumnos
	.map(function(a){
		return a.edad;
	})
	.reduce(function(a,b) {
		return a + b;
	})

	



	//NUEVO TEMA: CONSUMIR DATOS
	//https://jsonplaceholder.typicode.com/


	//ES5
	var request = new XMLHttpRequest();
	request.open('GET', '/my/url', true);
	
	//ES6 
	fech()
	//axios es una buena sistema de peticiones https://github.com/axios/axios

	//Mostrar Datos de un json
	fetch('https://jsonplaceholder.typicode.com/users')
  .then(response => response.json())
	.then(json => console.log(json))

	/*
	const onComplete = responsve => response.json(); es EN6 
	const onComplete = function(responsve){   es EN5
		return response.json();
	}
	*/